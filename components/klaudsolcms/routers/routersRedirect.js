export const redirectToBuilderTypeSlug = (slug) => {
  router.push(`/admin/content-type-builder/${slug}`);
}

export const redirectToManagerEntitySlug = (slug) => {
  router.push(`/admin/content-manager/${slug}`)
}